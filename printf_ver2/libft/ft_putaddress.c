#include "libft.h"

int putaddress(void *ptr)
{
    int cnt_put_address;
    unsigned long int   address;

    address = 0;
    address = (unsigned long int)ptr;
    cnt_put_address = 0;
    while (address )

    return (cnt_put_address);
}